# Example environment variables for DevDashboard
# Set DEV_MODE to 'true' to use mock data without connecting to Azure services
DEV_MODE=true
# Optional: ADO_PAT fallback if Managed Identity is not available
ADO_PAT=your-pat
